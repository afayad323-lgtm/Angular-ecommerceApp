import { HilightCard } from './hilight-card';

describe('HilightCard', () => {
  it('should create an instance', () => {
    const directive = new HilightCard();
    expect(directive).toBeTruthy();
  });
});
